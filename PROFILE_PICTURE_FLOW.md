# Profile Picture Upload Flow - Visual Guide

## 🎯 Complete Upload Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER CLICKS PROFILE PICTURE                   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  _updateProfilePicture() called in home_page.dart               │
│  → Shows "Opening image picker..." message                      │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  StorageService.pickImageFromGallery()                          │
│  → Opens file picker dialog                                     │
│  → User selects image                                           │
│  → Image compressed (max 1024x1024, quality 85%)                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
        ┌──────────────┐        ┌──────────────┐
        │ Image = null │        │ Image selected│
        │ (cancelled)  │        │               │
        └──────┬───────┘        └──────┬────────┘
               │                       │
               ▼                       ▼
    ┌──────────────────┐    ┌─────────────────────────────────┐
    │ Show "No image   │    │ Validate user is logged in      │
    │ selected"        │    │ Get userId from AuthProvider    │
    │ → Return         │    └──────────┬──────────────────────┘
    └──────────────────┘               │
                                       ▼
                            ┌─────────────────────────────────┐
                            │ Show "Uploading..." message     │
                            └──────────┬──────────────────────┘
                                       │
                                       ▼
                            ┌─────────────────────────────────────────┐
                            │ StorageService.uploadProfilePicture()   │
                            │ 1. Create storage reference             │
                            │    profilePictures/{userId}/{timestamp} │
                            │ 2. Read image as bytes                  │
                            │ 3. Upload to Firebase Storage           │
                            │ 4. Get download URL                     │
                            │ 5. Update Firestore user document       │
                            └──────────┬──────────────────────────────┘
                                       │
                        ┌──────────────┴──────────────┐
                        │                             │
                        ▼                             ▼
            ┌──────────────────┐          ┌──────────────────────┐
            │ Upload FAILED    │          │ Upload SUCCESS       │
            │ → Show error     │          │ → Get download URL   │
            │ → Log to console │          └──────┬───────────────┘
            └──────────────────┘                 │
                                                 ▼
                                    ┌────────────────────────────┐
                                    │ Update Firestore           │
                                    │ users/{userId}             │
                                    │ → profilePictureURL: url   │
                                    └──────┬─────────────────────┘
                                           │
                                           ▼
                                    ┌────────────────────────────┐
                                    │ AuthProvider.refreshUserData()│
                                    │ → Reload user from Firestore │
                                    │ → Update UI                  │
                                    └──────┬─────────────────────┘
                                           │
                                           ▼
                                    ┌────────────────────────────┐
                                    │ Show "Profile picture      │
                                    │ updated successfully!"     │
                                    │ → Image displays in UI     │
                                    └────────────────────────────┘
```

---

## 🗂️ File Structure

```
lib/
├── screens/
│   └── faculty/
│       └── home_page.dart
│           └── _updateProfilePicture()  ← Handles click event
│
├── services/
│   └── storage_service.dart
│       ├── pickImageFromGallery()       ← Opens file picker
│       ├── uploadProfilePicture()       ← Uploads to Firebase
│       └── _updateProfilePictureUrl()   ← Updates Firestore
│
├── providers/
│   └── auth_provider.dart
│       └── refreshUserData()            ← Reloads user data
│
└── widgets/
    └── profile_picture_widget.dart      ← Displays image
        └── _buildImage()                ← Shows URL or placeholder
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    UPLOAD REQUEST                            │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
            ┌───────────────────────┐
            │ Check Authentication  │
            │ request.auth != null? │
            └───────┬───────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
        ▼                       ▼
    ┌────────┐            ┌─────────────┐
    │  NO    │            │    YES      │
    │ DENY   │            │ Continue... │
    └────────┘            └──────┬──────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │ Check User ID Match    │
                    │ auth.uid == userId?    │
                    └──────┬─────────────────┘
                           │
                ┌──────────┴──────────┐
                │                     │
                ▼                     ▼
           ┌────────┐          ┌──────────┐
           │  NO    │          │   YES    │
           │ DENY   │          │ ALLOW    │
           └────────┘          └──────────┘
```

**Firebase Storage Rules:**
```javascript
match /profilePictures/{userId}/{allPaths=**} {
  allow read: if true;  // Anyone can view
  allow write: if request.auth != null && request.auth.uid == userId;  // Only owner can upload
}
```

---

## 📊 Data Flow

```
┌──────────────┐
│   Flutter    │
│     App      │
└──────┬───────┘
       │
       │ 1. User selects image
       │
       ▼
┌──────────────────┐
│  Image Picker    │
│  (Local Device)  │
└──────┬───────────┘
       │
       │ 2. Image data (bytes)
       │
       ▼
┌──────────────────────┐
│  Firebase Storage    │
│  profilePictures/    │
│    {userId}/         │
│      {timestamp}.jpg │
└──────┬───────────────┘
       │
       │ 3. Download URL
       │
       ▼
┌──────────────────────┐
│   Cloud Firestore    │
│   users/{userId}     │
│   profilePictureURL  │
└──────┬───────────────┘
       │
       │ 4. Updated user data
       │
       ▼
┌──────────────────┐
│  AuthProvider    │
│  (State Mgmt)    │
└──────┬───────────┘
       │
       │ 5. UI Update
       │
       ▼
┌──────────────────────┐
│ ProfilePictureWidget │
│ (Displays Image)     │
└──────────────────────┘
```

---

## 🌐 Web vs Mobile Differences

### Web (Chrome)
```
Image Picker → Browser File Dialog
             ↓
         XFile (bytes)
             ↓
    Firebase Storage (CORS required!)
             ↓
         Download URL
```

### Mobile (Android/iOS)
```
Image Picker → Native Gallery/Camera
             ↓
         XFile (file path)
             ↓
    Firebase Storage (No CORS needed)
             ↓
         Download URL
```

**Key Difference:** Web requires CORS configuration!

---

## 🐛 Debug Points

Add `print()` statements at these points:

```dart
// 1. Click handler
print('Profile picture clicked');

// 2. Image picker
print('Opening image picker...');
print('Image selected: ${image?.name}');

// 3. Upload start
print('Starting upload for user: $userId');

// 4. Upload progress
print('Image data read: ${data.length} bytes');
print('Upload task started...');

// 5. Upload complete
print('Download URL: $downloadUrl');

// 6. Firestore update
print('Firestore updated');

// 7. UI refresh
print('User data refreshed');
```

---

## ✅ Verification Checklist

After upload, verify:

```
□ Console shows: "Profile picture uploaded successfully"
□ Firebase Console → Storage → Files shows new image
□ Firebase Console → Firestore → users/{userId} has profilePictureURL
□ Image displays in app
□ Image URL is accessible (open in browser)
□ No errors in console
```

---

## 🔄 State Management Flow

```
┌────────────────┐
│  User Action   │
│ (Click picture)│
└───────┬────────┘
        │
        ▼
┌────────────────────┐
│   home_page.dart   │
│ _updateProfilePic()│
└───────┬────────────┘
        │
        ▼
┌────────────────────┐
│ StorageService     │
│ uploadProfilePic() │
└───────┬────────────┘
        │
        ▼
┌────────────────────┐
│ Firebase Storage   │
│ (Cloud)            │
└───────┬────────────┘
        │
        ▼
┌────────────────────┐
│ Cloud Firestore    │
│ Update user doc    │
└───────┬────────────┘
        │
        ▼
┌────────────────────┐
│  AuthProvider      │
│ refreshUserData()  │
│ notifyListeners()  │
└───────┬────────────┘
        │
        ▼
┌────────────────────┐
│  UI Rebuild        │
│ (Consumer/Provider)│
└────────────────────┘
```

---

## 🎨 UI States

```
┌─────────────────┐
│   Initial       │
│ (No picture)    │
│ Shows: Icon     │
└────────┬────────┘
         │
         │ User uploads
         │
         ▼
┌─────────────────┐
│   Loading       │
│ Shows: Spinner  │
└────────┬────────┘
         │
         │ Upload complete
         │
         ▼
┌─────────────────┐
│   Loaded        │
│ Shows: Image    │
└─────────────────┘
```

**ProfilePictureWidget handles all states automatically!**

---

This visual guide shows the complete flow from user click to image display! 🎉
